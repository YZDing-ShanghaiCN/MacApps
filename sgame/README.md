V# sgame

一个纯前端小游戏合集，包含舒尔特方格、扫雷、吃豆人、2048 和俄罗斯方块。

## 安装

```bash
conda activate sgame
pip install -r sgame/requirements.txt
```

## 本地游玩

```bash
python sgame/scripts/run_server.py
```

在浏览器打开 [http://127.0.0.1:8001](http://127.0.0.1:8001)，从首页选择游戏进入。

## 公网访问

首次使用先安装 Cloudflare 客户端（例如从包管理器或官方发布页下载二进制），然后运行：

```bash
python sgame/scripts/run_quick_tunnel.py
```

终端会输出一个 `https://*.trycloudflare.com` 地址，任意设备打开即可访问。

- 终端必须一直保持运行；关闭后服务中断。
- 每次重启都会生成新地址。

## 游戏说明

### 舒尔特方格

提供 3×3（1-9）、4×4（1-16）、5×5（1-25）三种模式，方格中随机分布数字，按顺序尽快点击所有数字。
第一次点击开始计时，点错会记录错误次数。完成时显示用时和错误次数，每种模式的最佳成绩分别保存在浏览器本地存储中。

### 扫雷

经典扫雷，主菜单包含开始游戏、排行榜、规则说明与个人战绩（总局数 / 胜局 / 胜率）：

- 初级：9×9，10 个地雷
- 中级：16×16，40 个地雷
- 高级：16×30，99 个地雷

进入游戏后：左键翻开格子，右键（或开启插旗模式、手机长按）插旗，工具栏提供独立的「重新开始」按钮。第一次点击保证安全。点击已翻开的数字且周围旗数匹配，可一次翻开周围剩余格子。胜利用时自动计入本地排行榜（每种难度保存最快前十名）。

### 吃豆人

经典街机吃豆人。方向键 / WASD 移动，手机上在画面内滑动。共 4 个挑战关卡：地图越来越大、幽灵越来越多（2→3→4→4 只），速度与能量豆时长各关一致；通关第 4 关即获胜。生命 3 条，最高分保存在浏览器本地存储中。

### 2048

经典 4×4 数字合并游戏。方向键 / WASD 移动，手机上滑动操作。相同数字合并，目标合成 2048。最高分保存在浏览器本地存储中，达到 2048 后可选择继续。

### 俄罗斯方块

经典落下式拼块游戏。A / D 左右移动，鼠标左键旋转方块，按住左 Shift 加速下落；手机上显示屏幕下方 4 个按钮（左 / 右 / 旋转 / 加速），按住可连续操作。消除 1-4 行分别得 100 / 300 / 500 / 800 × 当前关卡，每消除 10 行升一级并加快下落速度。最高分保存在浏览器本地存储中。

## 测试

```bash
pytest sgame/tests/
```

## Docker

在仓库根目录（`MacApps/`）执行：

```bash
docker build -f sgame/Dockerfile -t sgame .
docker run -d -p 8001:8001 sgame
```